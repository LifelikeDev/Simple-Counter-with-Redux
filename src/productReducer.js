import { SET_LOADING, GET_PRODUCTS } from "./action-variables";

// init defaultState
const defaultState = {
  name: "productReducer",
};

// reducer function
function reducer(state = defaultState, action) {
  return state;
}

export default reducer;
